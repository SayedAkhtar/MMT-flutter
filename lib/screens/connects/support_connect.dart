import 'package:flutter/material.dart';

class SupportConnect extends StatefulWidget {
  const SupportConnect({Key? key}) : super(key: key);

  @override
  State<SupportConnect> createState() => _SupportConnectState();
}

class _SupportConnectState extends State<SupportConnect> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
